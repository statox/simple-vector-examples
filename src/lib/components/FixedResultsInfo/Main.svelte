<p>
    Note that in this page the precision of the results is fixed by applying
    <a
        href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toFixed"
    >
        <code>.toFixed()</code>
    </a>
    to the output of the methods. When you use theses methods in your code you will get the raw results
    without a fixed precision.
</p>

<style>
    p {
        padding: 0.5rem;
        margin-block-start: var(--gap);
        background-color: var(--box-bg);
    }
</style>
